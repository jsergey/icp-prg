package ru.scrumtrek.trainings.icpprg.badcode;


public class DuplicateCode {

    /*
    mPillInteraction.setOnClickListener(new View.OnClickListener() {
        @Override
        public void onClick(View v) {
            mInteractionExpandable.setTrim(isInteraction);
            isInteraction = !isInteraction;
            mShadow_9.setVisibility(isInteraction ? View.VISIBLE : View.GONE);
            mInteractionExpandable.setVisibility(isInteraction ? View.VISIBLE : View.GONE);
            mInteractionExpandable.setText(mInteractionExpandable.getOriginalText());
            setViewController(mController_9, isInteraction);
        }
    })
    mPillOverdose.setOnClickListener(new View.OnClickListener() {
        @Override
        public void onClick(View v) {
            mOverdoseExpandable.setTrim(isOverdose);
            isOverdose = !isOverdose;
            mShadow_10.setVisibility(isOverdose ? View.VISIBLE : View.GONE);
            mOverdoseExpandable.setVisibility(isOverdose ? View.VISIBLE : View.GONE);
            mOverdoseExpandable.setText(mOverdoseExpandable.getOriginalText());
            setViewController(mController_10, isOverdose);
        }
    })
    mPillCautions.setOnClickListener(new View.OnClickListener() {
        @Override
        public void onClick(View v) {
            mCautionsExpandable.setTrim(isCautions);
            isCautions = !isCautions;
            mShadow_11.setVisibility(isCautions ? View.VISIBLE : View.GONE);
            mCautionsExpandable.setVisibility(isCautions ? View.VISIBLE : View.GONE);
            mCautionsExpandable.setText(mCautionsExpandable.getOriginalText());
            setViewController(mController_11, isCautions);
        }
    })
    mPillExp.setOnClickListener(new View.OnClickListener() {
        @Override
        public void onClick(View v) {
            mExpExpandable.setTrim(isExp);
            isExp = !isExp;
            mShadow_12.setVisibility(isExp ? View.VISIBLE : View.GONE);
            mExpExpandable.setVisibility(isExp ? View.VISIBLE : View.GONE);
            mExpExpandable.setText(mExpExpandable.getOriginalText());
            setViewController(mController_12, isExp);
        }
    })
    */


}
